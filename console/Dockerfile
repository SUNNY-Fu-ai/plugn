FROM yiisoftware/yii2-php:8.2-apache

# The Apache compose stack declares a console service. Keep it on the same
# PHP runtime as the web services so Composer can install the PHP >=8.2 app.
WORKDIR /app
